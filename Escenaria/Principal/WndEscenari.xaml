<Window x:Class="Principal.WndEscenari"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Escenari"
        Width="800" Height="600" Closing="Window_Closing" Background="{StaticResource ResourceKey=FonsGradientResource}">
    <Viewbox HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid x:Name="ugPista" Style="{StaticResource ResourceKey=gridEscenariStyle}"></Grid>
            <Grid Width="auto">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button x:Name="btnCicle" Grid.Column="2" FontSize="50" Content="Cicle" Click="Button_Click" BorderThickness="0"/>
                <Button x:Name="btnSimpaties" Grid.Column="0" FontSize="50" Content="Obrir Simpaties" Click="btnSimpaties_Click"/>
                <Button x:Name="btnNouEscenari" Grid.Column="1" FontSize="50" Content="Nova Festa" Click="btnNouEscenari_Click"/> 
            </Grid>
        </StackPanel>
    </Viewbox>
</Window>
